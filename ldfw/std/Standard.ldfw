#> Standard

// Layout padrão LDFW: stnd
// (o layout efetivo é definido dentro do bloco Docs do seu projeto:
//  Docs: { layout #> stnd; ... })

// Componentes disponíveis:
// hero, grid, card, code, alert (tip/info/warning/danger/important),
// table, table config, h1-h3, p, ul, ol, faq
